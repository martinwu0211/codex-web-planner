engine/node_modules/
# The published plugin ships compiled engine/dist so a clean user does not
# need devDependencies such as tsx just to start the bundled CLI.
engine/.tooling/
engine/.pnpm-store/
*.log
.DS_Store
.env
.env.*
!.env.example
.secret
__pycache__/
coverage/
